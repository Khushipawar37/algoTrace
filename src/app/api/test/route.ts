import { NextResponse } from "next/server";
export async function POST(){return NextResponse.json({error:{code:"GONE",message:"Legacy in-process execution is disabled. Use /api/problems/[slug]/run."}},{status:410});}