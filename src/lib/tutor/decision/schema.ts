import { z } from "zod";
export const TutorActionSchema = z.enum(["ASK_REASONING_QUESTION", "ASK_COMPLEXITY_QUESTION", "ASK_DRY_RUN", "POINT_TO_CODE_REGION", "POINT_TO_FAILURE_TYPE", "EXPLAIN_CONCEPT", "COMPARE_ALTERNATIVES", "GUIDE_EDGE_CASE_REASONING", "GUIDE_BOUNDARY_REASONING", "GUIDE_STATE_REASONING", "GUIDE_COMPLEXITY_IMPROVEMENT", "PROVIDE_APPROACH_DIRECTION", "PROVIDE_PSEUDOCODE", "PROVIDE_CODE_FRAGMENT", "ASK_STUDENT_TO_TRY_AGAIN", "ASK_REFLECTION", "ACKNOWLEDGE_SUCCESS", "CLARIFY_STUDENT_QUESTION", "SAFE_FALLBACK"]);
export const TutorDecisionSchema = z.object({ action: TutorActionSchema, objective: z.string().min(1).max(300), focus: z.string().max(200).optional(), targetIssue: z.string().max(300).optional(), targetCodeRegion: z.object({ startLine: z.number().int().positive().optional(), endLine: z.number().int().positive().optional(), description: z.string().min(1).max(300) }).optional(), reasoningMode: z.enum(["SOCRATIC", "EXPLANATORY", "DIAGNOSTIC", "REFLECTIVE"]).optional(), recommendedPromptStyle: z.string().max(300).optional(), shouldReferenceExecution: z.boolean(), shouldReferenceCode: z.boolean(), shouldAskForAnotherAttempt: z.boolean(), confidence: z.number().finite().transform((value) => Math.max(0, Math.min(1, value))) }).strict();
